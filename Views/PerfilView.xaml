<UserControl x:Class="Cajolote.Views.PerfilView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
        <Style x:Key="InputControlBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="#FF94A2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="5,0,5,15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#00000020" BlurRadius="10" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <ScrollViewer x:Name="MainScrollViewer" Focusable="True" FocusVisualStyle="{x:Null}" Padding="30" VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Perfil del Negocio" FontSize="28" FontWeight="Bold" Foreground="#4A3B47"/>
                <TextBlock Text="Esta información se mostrará en los tickets de venta" FontSize="14" Foreground="#6B5A68"/>
            </StackPanel>
            
            <!-- Foto e info general -->
            <Border Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0" VerticalAlignment="Center">
                        <!-- Contenedor de la Imagen con su botón de edición en la esquina -->
                        <Grid Width="90" Height="90" ClipToBounds="False" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <!-- Círculo de la imagen -->
                            <Border Width="90" Height="90" CornerRadius="45" Background="#FF94A2">
                                <Grid>
                                    <!-- Placeholder emoji -->
                                    <TextBlock Text="🏪" FontSize="45" HorizontalAlignment="Center" VerticalAlignment="Center"
                                               Visibility="{Binding ShowPlaceholder, Converter={StaticResource BoolToVis}}"/>
                                    <!-- Profile image -->
                                    <Ellipse Width="90" Height="90" Visibility="{Binding HasProfileImage, Converter={StaticResource BoolToVis}}">
                                        <Ellipse.Fill>
                                            <ImageBrush ImageSource="{Binding ProfileImageSource}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                </Grid>
                            </Border>

                            <!-- Botón de edición (Lápiz) con su badge de atajo -->
                            <Grid HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-3, -3" ClipToBounds="False">
                                <ToggleButton x:Name="EditImageToggleButton" Style="{x:Null}" Width="27" Height="27" Cursor="Hand" Background="#E0EBFF" BorderThickness="0">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="15">
                                                <materialDesign:PackIcon Kind="Pencil" Width="16" Height="16" Foreground="#4361EE" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>

                                <!-- Badge con el atajo Ctrl+I flotando -->
                                <Border HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-7,-20,0" Background="#EDE7E7" BorderBrush="#DCD4D4" BorderThickness="1" CornerRadius="4" Padding="3,1" IsHitTestVisible="False">
                                    <TextBlock Text="Ctrl+I" FontSize="8" FontWeight="SemiBold" Foreground="#6B707E"/>
                                </Border>
                            </Grid>
                        </Grid>

                        <!-- Cuadro desplegable a la derecha con los 2 botones -->
                        <Popup x:Name="EditImagePopup" IsOpen="{Binding ElementName=EditImageToggleButton, Path=IsChecked, Mode=TwoWay}" PlacementTarget="{Binding ElementName=EditImageToggleButton}" Placement="Right" AllowsTransparency="True" PopupAnimation="Fade" StaysOpen="False">
                            <Border Padding="12,10" Background="White" BorderBrush="#E8C4D8" BorderThickness="1.5" CornerRadius="8" Margin="8,0,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="1" Opacity="0.1"/>
                                </Border.Effect>
                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
                                        <Button Style="{x:Null}" Content="📷 Cambiar" Command="{Binding SelectImageCommand}" Click="ActionButton_Click" FontSize="10" Padding="8,4" Background="#E0EBFF" Foreground="#4361EE" BorderThickness="0" Cursor="Hand">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                        <Border Style="{StaticResource KeyboardShortcutStyle}" Margin="8,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="Ctrl+A" Style="{StaticResource KeyboardShortcutTextStyle}"/>
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Visibility="{Binding HasProfileImage, Converter={StaticResource BoolToVis}}">
                                        <Button Style="{x:Null}" Content="🗑️ Eliminar" Command="{Binding RemoveImageCommand}" Click="ActionButton_Click" FontSize="10" Padding="8,4" Background="#FFE3E3" Foreground="#E5383B" BorderThickness="0" Cursor="Hand">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                        <Border Style="{StaticResource KeyboardShortcutStyle}" Margin="8,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="Ctrl+Supr" Style="{StaticResource KeyboardShortcutTextStyle}"/>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </Popup>
                    </Grid>
                    
                    <StackPanel Grid.Column="1" Margin="40,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding StoreName, TargetNullValue='Nombre de Tienda'}" FontSize="22" FontWeight="Bold" Foreground="#4A3B47"/>
                        <TextBlock Text="{Binding Rfc, TargetNullValue='RFC'}" FontSize="14" Foreground="#6B5A68" Margin="0,4,0,0"/>
                        <TextBlock FontSize="12" Foreground="#4361EE" Margin="0,4,0,0" Text="Cajolote Versión Beta 0.9.5"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Información personal -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="Información de la Tienda" FontSize="16" FontWeight="Bold" Foreground="#4A3B47" VerticalAlignment="Center"/>
                        <Border Style="{StaticResource KeyboardShortcutStyle}" Margin="8,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Ctrl+E" Style="{StaticResource KeyboardShortcutTextStyle}"/>
                        </Border>
                    </StackPanel>

                    <!-- Campo del Propietario (No se imprime en tickets, solo para Firestore) -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center" Margin="0,0,10,0">
                            <TextBlock Text="Nombre Completo" FontSize="13" FontWeight="Bold" Foreground="#4A3B47"/>
                            <TextBlock Text="(Este dato NO se imprime en tickets, los de abajo si.)" FontSize="10" Foreground="#4361EE" Opacity=".8" TextWrapping="Wrap"/>
                        </StackPanel>
                        <Border Grid.Column="1" Style="{StaticResource InputControlBorderStyle}">
                            <TextBox x:Name="OwnerNameTextBox" Text="{Binding OwnerName, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="10,8" BorderThickness="0" Background="Transparent" materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"/>
                        </Border>
                    </Grid>

                    <Separator Background="#E8C4D8" Height="1" Margin="0,0,0,15"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Text="Nombre de Tienda*" FontSize="13" Foreground="#6B5A68" VerticalAlignment="Center"/>
                        <Border Grid.Column="1" Style="{StaticResource InputControlBorderStyle}">
                            <TextBox x:Name="StoreNameTextBox" Text="{Binding StoreName, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="10,8" BorderThickness="0" Background="Transparent" materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"/>
                        </Border>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="RFC (opcional)" FontSize="13" Foreground="#6B5A68" VerticalAlignment="Center"/>
                        <Border Grid.Column="1" Style="{StaticResource InputControlBorderStyle}">
                            <TextBox x:Name="RfcTextBox" Text="{Binding Rfc, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="10,8" BorderThickness="0" Background="Transparent" materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"/>
                        </Border>
                    </Grid>
                    
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Teléfono (opcional)" FontSize="13" Foreground="#6B5A68" VerticalAlignment="Center" Margin="-2,0,2,0"/>
                        <Border Grid.Column="1" Style="{StaticResource InputControlBorderStyle}">
                            <TextBox x:Name="PhoneTextBox" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="10,8" BorderThickness="0" Background="Transparent" materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"/>
                        </Border>
                    </Grid>
                    
                    <Grid Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Dirección (opcional)" FontSize="13" Foreground="#6B5A68" VerticalAlignment="Top" Margin="0,0,8,0"/>
                        <Border Grid.Column="1" Style="{StaticResource InputControlBorderStyle}">
                            <TextBox x:Name="AddressTextBox" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="10,8" BorderThickness="0" Background="Transparent" TextWrapping="Wrap" Height="50" materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"/>
                        </Border>
                    </Grid>
                    
                    <Grid Margin="0,15,0,0">
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Left">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                                <TextBlock Text="Estado:" FontSize="13" Foreground="#6B5A68" FontWeight="SemiBold" Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding SyncStatusText}" FontSize="13" Foreground="{Binding SyncStatusColor}" FontWeight="Bold"/>
                            </StackPanel>
                            <TextBlock Text="{Binding LastUpdatedText, StringFormat='Última sincronización: {0}'}" FontSize="11" Foreground="#9A8A97"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock Text="{Binding StatusMessage}" FontSize="13" Foreground="#76C893" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <Button Content="Guardar Cambios" Command="{Binding SaveProfileCommand}" FontSize="16" Padding="15,5" Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#D8F3DC"/>
                                        <Setter Property="Foreground" Value="#2D6A4F"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" SnapsToDevicePixels="True">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSavePending}" Value="True">
                                                <Setter Property="Background" Value="#95D5A4"/>
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.08" Duration="0:0:0.18"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.08" Duration="0:0:0.18"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                                <DataTrigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.18"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.18"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.ExitActions>
                                            </DataTrigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Opacity" Value="0.9"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Border Style="{StaticResource KeyboardShortcutStyle}" Margin="8,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Ctrl+S" Style="{StaticResource KeyboardShortcutTextStyle}"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
            

            
            <!-- Cerrar sesión -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,20">
                <Border CornerRadius="8" Width="250">
                    <Button Content="Cerrar Sesión" Background="#FFE3E3" Foreground="#E5383B" FontSize="16" Padding="15,5" BorderThickness="0" Cursor="Hand"
                            Command="{Binding LogoutCommand}"/>
                </Border>
                <Border Style="{StaticResource KeyboardShortcutStyle}" Margin="8,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Ctrl+Shift+Z" Style="{StaticResource KeyboardShortcutTextStyle}"/>
                </Border>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
